Полный стек EDA
Ingestion
PostgreSQL (CDC via Debezium + Vault Dynamic Credentials)
Messaging
Strimzi Kafka (mTLS via cert-manager → mathtrail-internal-ca-issuer) — Kafka ACLs (KafkaUser per component) — Apicurio Registry (Schema Evolution: FULL compatibility, все поля с default) — CloudEvents — Redpanda Console — DLQ & Error Handling
Stream Processing
Apache Flink — Stateful Processing (RocksDB + MinIO) — Flink SQL / Catalog — Watermarks & Late Data Handling — Data Quality (inline валидация → невалидные события → DLQ)
Sink
Kafka Topic (Flink → Kafka) — Microservices (Kafka Client Consumer, mTLS + ACLs, Static Membership, 1 pod = 1 partition) — Идемпотентность: Redis deduplication by event_id / Postgres ON CONFLICT / at-least-once для stateless workers
Security
HashiCorp Vault (K8s Auth + Dynamic DB Secrets + Transit KMS) — VCO / VSO — cert-manager (mathtrail-internal-ca-issuer → Strimzi mTLS, Apicurio TLS) — Kafka ACLs (KafkaUser per component)
Observability
OpenTelemetry (трейсы → Tempo, метрики → Prometheus) — Loki (Zap → Loki для Go, Log4j2 → Loki для JVM) — Grafana (LGTM стек + k6) — Pyroscope
