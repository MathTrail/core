Визуализация и отчетность производительности в GitHub Step Summary
Цель: Сделать результаты тестов наглядными, чтобы прямо в Pull Request видеть задержки (latency) и пропускную способность (throughput) всей цепочки (API -> Flink -> Kafka).

Запрос для Claude:

«Нам нужно добавить визуализацию результатов нагрузочного тестирования k6 в наш GitHub Actions Workflow. Мы хотим видеть отчет прямо в Step Summary каждого билда.

Задачи:

k6 Output: Настрой k6 так, чтобы он сохранял результаты теста в формате JSON (--out json=results.json).

Агрегация данных: Напиши небольшой Python или Node.js скрипт-парзер, который:

Извлекает ключевые метрики: http_req_duration (API latency) и нашу кастомную метрику kafka_e2e_latency (время пролета от API до выхода из Flink).

Считает процентили: p95 и p99.

Генерация графиков: Используй библиотеку для генерации графиков (например, mermaid или генерацию статического изображения через chart.js в CLI), чтобы отрисовать график распределения задержек.

GitHub Step Summary: >     * Сформируй Markdown-отчет и запиши его в $GITHUB_STEP_SUMMARY.

Отчет должен содержать:

Таблицу с основными показателями (RPS, Success Rate, Latency).

Mermaid-диаграмму gantt или bar chart, показывающую, сколько времени событие провело в каждой части системы (API -> DB -> Flink -> Kafka).

Статус: «🏎️ Ferrari Speed» (если p95 < 200ms) или «🐢 Tractor Mode» (если задержки выше нормы).

Alerting: Если kafka_e2e_latency превышает установленный порог (SLA), билд должен помечаться как «Warning» (желтый цвет в GitHub), даже если функциональные тесты прошли успешно.»

Как это будет работать физически:
k6 прогоняет сценарий. Благодаря тому, что ты используешь xk6-kafka, мы замеряем время с момента отправки HTTP-запроса до момента, когда consumer в k6 вычитал ответ из финального топика Kafka.

Скрипт-парзер берет этот лог и превращает его в красивую таблицу.

В интерфейсе GitHub на вкладке Summary ты видишь:

API Latency: 15ms

Flink Processing Time: 45ms

Total E2E Latency: 60ms

Status: ✅ Optimized

Почему это важно для MathTrail:
Когда ты начнешь внедрять AI-логику во Flink, ты сразу заметишь, как растет Processing Time. Если твоя LLM-модель начнет «тупить», график в PR резко пойдет вверх. Это позволит тебе оптимизировать код еще до того, как изменения попадут в основную ветку.